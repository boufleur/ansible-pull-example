---
- name: set if x
  set_fact:
    var_a: abc
  when: x is defined and x == 'y'
  
- name: set if not x
  set_fact:
    var_a: cde
  when: x is not defined
  
- name: import playbook
  import_playbook: local.yml
